const config = {
    application: {
        cors: {
            server: [
                {
                    origin: "localhost:3000",
                    credential: true
                }
            ]
        }
    }
}